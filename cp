Shell command used for copying files.
Takes as its first argument a source file which is copied to a target file given as its second argument.
Uses the "-R" switch to recursively copy an entire directory with the full contents of any subtree.
